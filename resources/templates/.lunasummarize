# yaml
# LUNA Summarize Configuration
# Controls which files get analyzed for AI-powered code summaries

# ═══════════════════════════════════════════════════════════════
# WHAT LUNA DOES: Quick Reference Encyclopedia
# ═══════════════════════════════════════════════════════════════
# LUNA generates summaries with:
#   • Purpose/description of each file
#   • Components with exact line numbers
#   • Imports and dependencies
#   • Inputs/outputs/returns for functions
#   • Links for easy navigation
#
# VALUE: A 2000-line module with a 50-line method becomes a 3-line
# summary entry - no context lost, huge readability gain.
#
# This is ONLY useful for files with executable logic - code that
# has behavior to explain. Config files, docs, and data files are
# better read directly (summaries lose precision).
# ═══════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════
# DECISION FRAMEWORK: What to include vs exclude
# ═══════════════════════════════════════════════════════════════
#
# ✅ INCLUDE: Files containing EXECUTABLE SOURCE CODE
#    - Business logic, algorithms, data structures
#    - Component implementations, handlers, services
#    - Custom utilities and helpers you wrote
#    - Ask: "Does this file define HOW the system works?"
#
# ❌ EXCLUDE: Everything else
#    - Dependencies (node_modules, vendor) - external code
#    - Generated files (.d.ts, compiled output) - not source of truth
#    - Build artifacts (dist/, build/) - outputs, not inputs
#    - Tests (.test.*, .spec.*) - examples, not core logic
#    - Documentation (.md, .txt) - metadata about code
#    - Config files (.json, .yaml, .toml) - read directly when needed
#      * package.json, tsconfig.json = reference data, not logic
#      * Summarizing configs loses fidelity - read them whole instead
#    - Lock files (package-lock.json) - auto-generated dependency snapshots
#
# RULE OF THUMB: If you'd skip it during a code review of the
# core business logic, exclude it here.
#
# CONFIG FILES: Don't summarize structured data files - read them
# directly when you need them. Summaries lose the precise details.
# ═══════════════════════════════════════════════════════════════

# File extensions to INCLUDE (whitespace-separated)
# List ONLY your primary source code language(s)
includeExtensions: ts tsx js jsx

# EXAMPLES FOR OTHER LANGUAGES:
# Python:           includeExtensions: py
# Java:             includeExtensions: java
# Rust:             includeExtensions: rs
# C/C++:            includeExtensions: c cpp h hpp
# Multi-language:   includeExtensions: ts py java

# ═══════════════════════════════════════════════════════════════
# AGENT REASONING GUIDE: When to add/remove file types
# ═══════════════════════════════════════════════════════════════
# Ask: "Does this file define executable logic or behavior?"
#
# YES → Include it (examples: .ts, .py, .java, .rs)
#   - Contains functions, classes, methods
#   - Implements algorithms or business logic
#   - Worth explaining in natural language
#
# NO → Exclude it (examples: .json, .md, .lock, .d.ts)
#   - Configuration data (read directly, don't summarize)
#   - Documentation (already human-readable)
#   - Generated artifacts (not source of truth)
#   - Type definitions (metadata, not logic)
#
# EDGE CASES:
# - .sql files: Include if they contain stored procedures/logic
# - .yaml files: Exclude (config data)
# - .tsx/.jsx: Include (components are logic)
# - .graphql files: Exclude (schema definitions, not logic)
# ═══════════════════════════════════════════════════════════════

# ═══════════════════════════════════════════════════════════════
# AGENT EDGE CASE REASONING: Evaluating ambiguous file types
# ═══════════════════════════════════════════════════════════════
# When encountering unfamiliar file types, ask:
# "Does this file define executable logic or behavior?"
#
# ✅ YES → Include (worth summarizing)
#   Characteristics:
#     - Contains functions, classes, methods, algorithms
#     - Implements business logic or behavioral rules
#     - Defines behavior that changes based on inputs/state
#     - Has conditional logic, loops, or data transformations
#     - Worth explaining in natural language
#   Examples: .ts, .py, .java, .rs, .go, .cpp, .rb, .php
#
# ❌ NO → Exclude (read directly instead)
#   Characteristics:
#     - Pure configuration or structured data
#     - Already human-readable documentation
#     - Generated artifacts (not source of truth)
#     - Type definitions or metadata only
#     - Declarative with no conditional logic
#   Examples: .json, .yaml, .toml, .md, .txt, .lock, .d.ts
#
# DECISION MATRIX FOR COMMON EDGE CASES:
#
# .sql files:
#   Simple queries (SELECT/INSERT) → EXCLUDE (data operations)
#   Stored procedures with IF/WHILE → INCLUDE (executable logic)
#
# .yaml/.json files:
#   Config files (package.json, tsconfig) → EXCLUDE (read whole)
#   CI/CD with complex conditionals → EXCLUDE (declarative)
#
# .tsx/.jsx files:
#   Components with hooks/state → INCLUDE (behavioral logic)
#   Pure presentational components → INCLUDE (render logic)
#
# .sh/.bash/.ps1 files:
#   Build scripts with conditionals → INCLUDE (automation)
#   Simple one-liners → EXCLUDE (trivial)
#
# .proto/.graphql/.gql files:
#   Schema definitions → EXCLUDE (declarative)
#
# .html/.css files:
#   Static markup/styles → EXCLUDE (presentational)
#   Templates with logic (Jinja, ERB) → INCLUDE (dynamic)
#
# WHEN UNCERTAIN: Favor EXCLUDE. Summaries lose precision on
# non-executable content. Better to read the original directly.
# ═══════════════════════════════════════════════════════════════

# Directories to EXCLUDE (glob patterns)
# Skip: dependencies, build outputs, environments, version control
excludePatterns:
  - node_modules/**
  - .git/**
  - dist/**
  - build/**
  - out/**
  - .venv/**
  - venv/**
  - __pycache__/**
  - .next/**
  - .nuxt/**
  - docs/**
  - .codebase/**
  - coverage/**

# Individual files to EXCLUDE (glob patterns)
# Skip: tests, type definitions, minified code, generated files, docs
excludeFiles:
  - **/*.test.ts
  - **/*.test.js
  - **/*.spec.ts
  - **/*.spec.js
  - **/*.min.js
  - **/*.min.css
  - **/*.d.ts
  - **/node_modules/**
  - package-lock.json
  - yarn.lock
  - pnpm-lock.yaml
  - **/*.md
  - **/*.mdx
  - **/*.json

# Maximum file size to analyze (in KB)
# Files larger than this are typically generated or vendored
maxFileSize: 500

# Enable verbose logging
# Useful for debugging which files are being included/excluded
verbose: false
